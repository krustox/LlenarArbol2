<%= form_for(enterprise) do |f| %>
  <% if enterprise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enterprise.errors.count, "error") %> prohibited this enterprise from being saved:</h2>

      <ul>
      <% enterprise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :empresa_id %>
    <%= f.number_field :empresa_id %>
  </div>

  <div class="field">
    <%= f.label :holding_id %>
    <%= f.number_field :holding_id %>
  </div>

  <div class="field">
    <%= f.label :nombre_empresa %>
    <%= f.text_field :nombre_empresa %>
  </div>

  <div class="field">
    <%= f.label :display_empresa %>
    <%= f.text_field :display_empresa %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
