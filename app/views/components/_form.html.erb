<%= form_for(component) do |f| %>
  <% if component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component.errors.count, "error") %> prohibited this component from being saved:</h2>

      <ul>
      <% component.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :componente_id %>
    <%= f.number_field :componente_id %>
  </div>

  <div class="field">
    <%= f.label :site_id %>
    <%= f.number_field :site_id %>
  </div>

  <div class="field">
    <%= f.label :nombre_componente %>
    <%= f.text_field :nombre_componente %>
  </div>

  <div class="field">
    <%= f.label :display_componente %>
    <%= f.text_field :display_componente %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
